# AnHeart Raspberry Pi Client Dependencies
#
# SYSTEM DEPENDENCIES (install before pip install):
# sudo apt install -y python3-dev libbluetooth-dev bluetooth bluez
#
# For Docker: No system deps needed, just run docker-compose up
#

# BITalino Bluetooth Classic communication
bitalino>=1.2.6
numpy>=1.20.0

# HTTP client
httpx>=0.27.0

# Configuration and validation
pydantic>=2.0.0
python-dotenv>=1.0.0

# Bluetooth auto-pairing (optional, for automatic PIN entry)
pexpect>=4.8.0

# Testing (not included in Docker image)
pytest>=8.0.0
pytest-asyncio>=0.23.0
